**Confirmed Bugs**
Critical: `/Volumes/external/sources/cppmega.mlx/scripts/eval_domain_routed_codegen.py:102` only compiles generated C++ and never runs the produced executable, while `/Volumes/external/sources/cppmega.mlx/scripts/eval_domain_routed_codegen.py:109` reports `compile_passed`. Failure mode: a wrong `add()` implementation still passes `cpp_docstring_add` as long as it links. Evidence: `/Volumes/external/sources/cppmega.mlx/evals/domain_routed_prompts.jsonl:1` has an executable `main()` oracle, but the harness never invokes `a.out`. Focused test/fix: add a completion returning `0`; assert eval fails after running the binary, then run `exe` and gate on exit code.

High: `/Volumes/external/sources/cppmega.mlx/scripts/cpp_jsonl_generation_compile_eval.py:97` makes `docstring` mode use `source_prefix`, identical to source-prefix mode; `/Volumes/external/sources/cppmega.mlx/tests/test_cpp_jsonl_generation_compile_eval.py:33` locks that bug in. Failure mode: “docstring conditioning” receipts are not docstring/instruction-only evals. Evidence: latest docstring receipt `/Volumes/external/sources/cppmega/outputs/evals/local_mlx_fixed_stage05_current_graph_routes_docstring_clang_greedy/completions.jsonl:1` is still generated from the full C++ prefix shape. Focused test/fix: make `docstring` extract only the leading comment/task text, keep `source-prefix` separate, and assert the two prompts differ.

High: `/Volumes/external/sources/cppmega.mlx/scripts/cpp_jsonl_generation_compile_eval.py:264` stops at any line starting with `}`; current H200 source has the same policy at `/Volumes/external/sources/cppmega/scripts/nebius_h200_megatron_cpp_generation_eval.py:231`. Failure mode: valid bodies with nested `if/for` blocks are truncated before trailing statements, producing unbalanced or semantically incomplete code. Evidence: all current local compile receipts sampled are `0/4`; e.g. `/Volumes/external/sources/cppmega/outputs/evals/local_mlx_fixed_stage05_current_graph_routes_docstring_clang_greedy/compile_report.json:8`. Focused test/fix: feed a valid body with an inner block and trailing `return`; trim must preserve it. Replace line-prefix trimming with brace-depth or remove it for body-only evals.

Medium: `/Volumes/external/sources/cppmega/scripts/cpp_generation_compile_eval.py:350` deletes `outputs/evals/cpp_generation_compile_work` when `--keep-workdir` is used. Failure mode: rerunning a receipt-preserving eval destroys the previous inspectable workdir. Evidence: line `/Volumes/external/sources/cppmega/scripts/cpp_generation_compile_eval.py:352` calls `shutil.rmtree(persistent)`. Focused test/fix: write each run under a timestamp/run-id dir and never delete unless an explicit `--replace-workdir` is supplied.

**Design Gaps**
High: H200 generation still does not build real prompt sidecars. `/Volumes/external/sources/cppmega/scripts/nebius_h200_megatron_cpp_generation_eval.py:391` creates all-zero structure inputs, and `/Volumes/external/sources/cppmega/scripts/nebius_h200_megatron_cpp_generation_eval.py:530` applies them during decode. Failure mode: sidecar-trained checkpoints see a distribution shift at eval time, which matches repeated/collapsed outputs. Focused fix: ship token-aligned prompt sidecars with each prompt or run the same clang side-channel builder remotely; fail if required sidecars are unavailable.

Medium: local generated suffix sidecars are always zeroed at `/Volumes/external/sources/cppmega.mlx/scripts/cpp_jsonl_generation_compile_eval.py:417`. Failure mode: after the prompt, generated code tokens lose structure/AST conditioning, encouraging collapse as the context grows. Focused test/fix: add an ablation matrix: prompt sidecars zero/clang x suffix zero/incremental-reparse, with compile pass rate and repetition metrics.

Medium: pass@k is not implemented. `/Volumes/external/sources/cppmega/scripts/cpp_generation_compile_eval.py:129` rejects duplicate `task_id`, so multiple candidates per task cannot be represented. Failure mode: all receipts are single-sample pass rate, not pass@k. Focused fix: add `candidate_id`/`sample_index`, allow repeated task ids, and compute pass@1/pass@5/pass@10 with deterministic seeds.

Medium: leakage-safe splits are not enforced. `/Volumes/external/sources/cppmega/scripts/cpp_generation_compile_eval.py:49` only requires `task_id`, `prompt`, `source_prefix`, and `source_suffix`; the eval set is only four rows at `/Volumes/external/sources/cppmega/evals/cpp_docstring_compile_cases.jsonl:1`. Failure mode: there is no provenance/hash check against training shards. Focused fix: require source/provenance hashes and fail if any eval prompt/source overlaps train manifests by repo/commit/file/function hash.

**Stale Artifacts**
Medium: the stored H200 receipt used an older generated worker. `/Volumes/external/sources/cppmega/outputs/nebius/cppmega-h200-generation-1782753301/generate_worker.py:55` only replaces `<SPACE>/<NL>`, while current source handles `<RESERVED_46>/<RESERVED_47>` at `/Volumes/external/sources/cppmega/scripts/nebius_h200_megatron_cpp_generation_eval.py:198`. Evidence: `/Volumes/external/sources/cppmega/outputs/nebius/cppmega-h200-generation-1782753301/completions.jsonl:1` leaks `<RESERVED_46>` into C++. Treat that receipt as stale for current-code decode behavior, but still useful evidence of tokenizer drift.

**Eval Matrix**
Trustworthy C/C++ matrix should be: source-prefix, docstring-only, FIM, IFIM x zero/clang prompt sidecars x zero/incremental suffix sidecars x greedy/sample pass@k. Each cell needs clang-format status, compile, link, executable run, timeout, stderr class, repetition metrics, and train-leakage hash status.

I did not edit files or run mutating tests. Verification here was read-only code/artifact inspection plus JSON/receipt parsing. No running data processes were touched. 

