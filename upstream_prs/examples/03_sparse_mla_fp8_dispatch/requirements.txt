# Stable CUDA 13.2 release used by the current H200/B200 stack.
# Install with --extra-index-url https://download.pytorch.org/whl/cu132.
torch==2.13.0+cu132
transformer-engine==2.13.0
# cppmega is the in-tree editable install:
#   pip install -e /path/to/cppmega
# It must be built against the same tilelang/tvm-ffi stack as the rest of
# the bench3 environment; otherwise SparseMLA / SparseMLA_FP8 will fail to
# import. See cppmega/README for build instructions.
#
# Also required at runtime (not pip-installable):
#   - TileLang dev build: /mnt/data/tilelang-build/build
#   - tvm-ffi < 0.1.10   (TileLang-compatible)
#   - CUDA 13.2 driver stack
